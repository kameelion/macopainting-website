<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title or "Maco Painting" }}</title>
  <meta name="description" content="{{ description or 'Expert Residential & Commercial Painting in Southeastern PA' }}">

  <!-- Preload critical hero images if provided -->
  {% if preloadImages %}
    {% for img in preloadImages %}
      <link rel="preload" as="image" href="{{ img }}" type="image/webp">
    {% endfor %}
  {% endif %}

  <!-- Preconnect for Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">

  <!-- Essential CSS -->
  <link rel="stylesheet" href="/styles/global.css">
  <link rel="stylesheet" href="/styles/navigation.css">
  <link rel="stylesheet" href="/styles/hero.css">
  <link rel="stylesheet" href="/styles/service-cards.css">

  <!-- Page-specific CSS (only what's needed!) -->
  {% if css %}
    {% for stylesheet in css %}
      <link rel="stylesheet" href="/styles/{{ stylesheet }}">
    {% endfor %}
  {% endif %}

  <!-- Inline LocalBusiness Schema for SEO -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "name": "Maco Painting",
    "url": "https://www.macopainting.com",
    "logo": "https://www.macopainting.com/logo.png",
    "image": "https://www.macopainting.com/images/exterior/exterior-house-painters-hero.webp",
    "description": "Maco Painting provides top-notch residential and commercial painting services in Southeastern PA. Specializing in interiors, exteriors, and epoxy flooring.",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "1288 Claridge Court",
      "addressLocality": "Coatesville",
      "addressRegion": "PA",
      "postalCode": "19320",
      "addressCountry": "US"
    },
    "telephone": "+1-484-467-5443",
    "priceRange": "$$ - $$$",
    "areaServed": [
      { "@type": "Place", "name": "Chester County" },
      { "@type": "Place", "name": "Delaware County" },
      { "@type": "Place", "name": "Montgomery County" },
      { "@type": "Place", "name": "Main Line" }
    ]
  }
  </script>
</head>

<body class="{{ pageClass or '' }}">
  <!-- Navigation (Inline, performance-optimized) -->
  <header class="site-header">
    <!-- Leave your existing nav HTML here (no changes, SVGs fine) -->
    <!-- ... -->
  </header>

  <!-- Main Content -->
  <main>
    {{ content | safe }}
  </main>

  <!-- Footer (Inline, same as now) -->
  <footer class="site-footer">
    <!-- Leave your existing footer HTML here (no changes) -->
    <!-- ... -->
  </footer>

  <!-- Navigation JS (deferred for non-blocking) -->
  <script src="/scripts/navigation.js" defer></script>

  <!-- Page-specific JS (if needed) -->
  {% if js %}
    {% for script in js %}
      <script src="/scripts/{{ script }}" defer></script>
    {% endfor %}
  {% endif %}
</body>
</html>
